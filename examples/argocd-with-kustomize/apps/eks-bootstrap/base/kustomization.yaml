apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/kloia/ArgoCD-EKS-Bootstrapper.git/bootstrap/base?ref=kustomize

# add new applications
# remove applications
# patch values
# change cluster destinations
# etc.